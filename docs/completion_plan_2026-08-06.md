# エンジン完成計画（2026-08-06 起点・429/630 → **2026-08-08 現在 561/630**）

> 進捗更新: 2026-08-08。Phase 0（回収）・Phase 1（曲線グラフ基盤）・Phase 4（C11 統計）が完了、
> Phase A（動点・速さ）・B（立体）・C（統計の word_problem）が完了。
> **Phase D（exam 融合）完了**（exam_l1〜l7 の 31セル）。exam の残りは exam_l6.proof Lv3 だけで、
> これは proof form ごと未実装なので Phase F に属する。
> **Phase E（端物）は 2/11 着地**（g3_l53.word_problem Lv4・g2_l33.find_value Lv2）。
> **word_problem は残り0**。残る端物9は graph_table 7・find_value 1・knowledge 1。
> 台帳は `PYTHONPATH=. .venv/bin/python -m engine.tools.coverage_page` で再生成（docs/coverage.html）。
> 未実装セル一覧は `PYTHONPATH=. .venv/bin/python scratchpad/list_gaps.py <unit接頭辞>`。
>
> **★進捗を更新する前に必ず `scratchpad/audit_progress.py` を回すこと**（exit 0 が条件）。
> 進捗更新では2方向のミスが繰り返し起きた。原因が違うので検査も別に要る:
> - **できていないのにできている**: Phase 1 が「完了」とされたが3束目は投入されて
>   いなかった → 完了と書いた Phase の対象セルを capabilities と突き合わせる
> - **できているのにできていない**: g1_l53 は solver も recipe もあるのに family が無く
>   台帳に載っていなかった／g1_l27 Lv4 の意図的除外が YAML に埋もれていた／
>   g3_l55・g3_l56 Lv4 は実装したのに「担当外」の記述が残っていた
>   → 未使用 recipe/solver・除外記述と gaps の突き合わせ を機械で検出する

## 0. いまの実測（2026-08-08・Phase D 完了 / Phase E 2/11）

`PYTHONPATH=. .venv/bin/python -m engine.tools.goal_progress` の出力そのまま
（C1〜C16 の振り分けは `docs/goal_spec_2026-07-12.md` §3 が定義し、このツールが計算する）。

```
  C1   g1 数と式（正負・文字式・一次方程式）          69/69  ( 100%)
  C2   g2 数と式（式の計算・連立）               32/32  ( 100%)
  C3   g3 数と式（多項式・平方根・二次方程式）         68/68  ( 100%)
  C4   g1 比例・反比例                     29/29  ( 100%)
  C5   g2 一次関数                       36/36  ( 100%)
  C6   g3 二次関数 y=ax²                 21/21  ( 100%)
  C7   g1 平面図形（作図以外）                 19/21  (  90%)
  C8   g1 空間図形                       24/24  ( 100%)
  C9   g2 図形（平行と合同・三角形と四角形）          40/44  (  91%)
  C10  g3 図形（相似・円・三平方）               50/51  (  98%)
  C11  データ・統計（g1 分布＋g2 箱ひげ＋g3 標本）    39/39  ( 100%)
  C12  確率（g1＋g2）                     14/16  (  88%)
  C13  exam 融合の T1 部                 20/20  ( 100%)
  C14  word_problem 全学年（T3）         100/100 ( 100%)
  C15  proof 全学年（T3）                  0/51  (   0%)
  C16  construction（g1 作図・M2）         0/9   (   0%)
  合計                                561/630 (89.0%)
  学年別: exam 34/35  g1 198/209  g2 156/197  g3 173/189
```

**C13（exam 融合の T1 部）と C14（word_problem 全学年）が 100% になった**。
残る 69 は C15 proof 51・C16 construction 9 と、C7/C9/C10/C12 に散った端物9セル。

### この数字がまだ保証していないもの（planning の前提として重要）

- **`写像監査 ≥95%` は一度も測っていない。** goal_spec §3.1 の完成条件
  G1-BASE-DONE は (a) green 630/630 ∧ (b) eval exit 0 ∧ **(c) 写像監査 ≥95%** の
  3つで、(c) は「外部の実問題が、この 630 セルのどれかに写せるか」を測る条件。
  測る道具が `engine/tools/` に無く、実施記録も無い。**セル数だけを見て完成と
  判断できない**のはここ。630 に届いたあと (c) を立ち上げる、では順序が逆になる
  （写せない問題が見つかれば台帳そのものが増減する）。
- **分母 630 は動きうる。** 台帳の `unit_flag` が `ok` でない単元が2つある——
  `g2_l15`（merge: g2_l14 へ統合すべき・1セル）と `g3_l53`（split:「特別な直角
  三角形の辺の比」を独立レッスンに分離すべき・6セル）。どちらも「将来こう直す」
  という宣言で、実施すればセル数が動く。いまは両方とも実装済みなので進捗の
  分子・分母は整合している。

### この数字が何を保証しているか（2026-08-08 に実測で確かめた）

`capabilities()` は「family が宣言していて lint が通る」セルを返すだけで、
**実際に問題が生成できるかは見ていない**。そこで 561 セル全部を 3 seed ずつ
実際に generate して確かめた（`scratchpad/verify_progress.py`）:

- 例外で落ちたセル **0**／`Unsupported` が返ったセル **0**
  → **561/561 が実際に生成できる**（宣言だけの「できている」は無い）
- 台帳・family 宣言・capabilities の三者に食い違い **0**
  （宣言があるのに台帳に無い／lint 落ち／台帳に無いセルを生成、いずれも 0 件）

**一次方程式・二次方程式は完成**（`g1_l21〜l27` / `g3_l24〜l31` がすべて 0 gaps。
最後まで残っていた g1_l27 Lv4・g3_l31 Lv3/Lv4 は Phase A で回収済み）。

## 1. 残り69セルの実態（2026-08-08 実測）

（「起点」列＝2026-08-06 にこの計画を書いた時点の残り 201 セルの内訳）

| form | 残り | 起点(201) | 性質 |
|---|---|---|---|
| proof | 51 | 51 | **form ごと未実装**（frame も solver も checker もゼロ）。手つかず |
| construction | 9 | 9 | **form ごと未実装**。M2 送り |
| graph_table | 7 | 58 | すべて端物 |
| find_value | 1 | 36 | g2_l50 Lv3（面積を2等分する直線） |
| word_problem | 0 | 36 | **完了** |
| knowledge | 1 | 10 | g2_l38 Lv2 のみ |

**exam は 34/35。残りは exam_l6.proof Lv3 の1セルだけで、これは proof form ごと
未実装なので Phase F に属する**。かつて「単元横断で32セルあり form 内訳に散っていて
見えない」と書いた塊は、Phase D で解消した。
**proof と construction を除くと、残りは端物9セルだけ**（graph_table 7・
find_value 1・knowledge 1）。

### 効いた手・尽きた手

- **効いた**: 描画資産を1本起こして横展開する形。
  `graph.py` の曲線（`kind=="curve"`）→ C4 12セル＋C6 8セルが一度に開通し両方 100%。
  `visuals/distribution_chart.py`（ヒストグラム/度数折れ線/累積/箱ひげ図）→ C11 17セル。
- **尽きた**: word_problem を form 単位で括る償却（`word_problem_linear.py` が6セル、
  `word_problem_quadratic.py` が4セル…という手）。残りは Lv3/Lv4 と exam で、
  一つずつ違う数学を要求する。
- **代わりに効く**: 「単元クラスタ × form 横断」。同じ場面が graph_table と
  word_problem の両方に出るので、場面ビルダー1つで2 form ぶん取れる。

---

## Phase 0：未マージの回収 — ✅ 完了（429 → 480）

サブエージェント4体分（C10 相似・円 find_value / C8 空間図形 計量 / C10 三平方
find_value / C8 空間図形 knowledge）を検証してマージ済み。
**11セル中5セルがゲートを通っていなかった**ため、失敗パターンを
`scratchpad/BRIEF_2026-08-06_v2.md` に明文化した。

## Phase 1：曲線グラフ基盤 → C4 + C6 — ✅ 完了

`visuals/graph.py` に `kind=="curve"`（サンプリング点列 → polyline）を追加。
放物線・双曲線・比例直線がすべて描けるようになり、**C4 29/29・C6 21/21 で両方完成**。

### ⚠ ただし3束目（動点・速さ word_problem）が落ちている

Phase 1 は並列3体で回す計画で、3束目が
「g3_l31 Lv3/Lv4・g3_l38 Lv4・g3_l37 Lv3/Lv4・g1_l36 Lv4・g1_l27 Lv4」＝
**優先事項だった一次方程式・二次方程式の残り3セルを含む束**だった。
実際に着地したのは 1束目（`0ac67e8` C4）と 2束目（`7245200` C6）だけで、
残りの枠は C8 knowledge（`a921c11`）と C10 find_value（`b632f6f`）に流れ、
**3束目は投入されないまま Phase 1 が「完了」扱いになった。**

→ **Phase A に繰り上げて最優先で回収した**（下記・4セルとも着地済み）。
再発防止として、束ごとに「どのコミットで着地したか」を必ず本書に記録する。

## Phase 4：C11 データ・統計 — ✅ 完了（480 → 497）

`visuals/distribution_chart.py` を新規に起こし（`math.frequency_chart` /
`math.box_plot`）、その上に17セル。

| コミット | 内容 | セル |
|---|---|---|
| `1057b12` | Phase4 基盤（分布のグラフ／箱ひげ図の描画） | — |
| `447e9f6` | 分布のグラフ graph_table（g1_l54/l55/l56/l58） | 10 |
| `8b60bf3` | 箱ひげ図 graph_table + word_problem（g2_l56/l57） | 7 |

**C11 は 39/39 で完成。** ただし g1_l58.word_problem Lv2/3/4（統計的探究プロセス）と
g3_l57/l58.word_problem Lv2（標本調査）は C14 側に計上されており未着手＝下記に残る。

---

# ここから先（この見出しを書いた時点の残り102。2026-08-08 現在の残りは 69）

## Phase A：動点・速さクラスタ（12セル）— ✅ 完了（497 → 509）

**Phase 1 の3束目として投入されるはずが落ちた束**（上記 ⚠ 参照）。
同じ「動く点」の場面を共有するので、1つの scene ビルダーで複数 form・複数単元を賄う。

| セル | 内容 | 着地 |
|---|---|---|
| g3_l31 word_problem Lv3/Lv4 | 2次方程式の利用（2点が動く／時刻をすべて求める） | ✅ `7e6de21` |
| g1_l27 word_problem Lv4 | 往復の道のりと平均の速さ（作問セルの再定義） | ✅ `75726a3` |
| g3_l38 word_problem Lv4 | 面積のグラフを構成してから時刻をすべて求める | ✅ `75726a3` |
| g2_l29 word_problem Lv3/Lv4 ＋ graph_table Lv3 | 動点と面積の変化（1次関数）＝同じ場面で2 form | ✅ `bd4ace8` |
| g3_l37 word_problem Lv3/Lv4 | 放物線と直線が交わる図形 | ✅ `bd4ace8` |
| g1_l36 Lv4・g2_l28 Lv4・g2_l30 Lv4 | 比例の利用・水そう・ダイヤグラム | ✅ `bd4ace8` |

**一次方程式・二次方程式はこれで完成**（`g1_l21〜l27` / `g3_l24〜l31` がすべて 0 gaps）。
`exam_l3`（動点と面積変化）も同じ機構に乗るので、Phase D が後段で安くなる。

### この束で確定した設計判断（後続セルはこれに従う）

1. **「問題をつくれ」型のセル（作問）は再定義して実装する。**
   設定は engine が固定し、採点対象は決定論で判定できる結論にする。
   書かせる部分は solution_steps が担う（箱ひげ図の記述セルと同じ手）。
   逸脱は family YAML の source_desc に必ず明記する。
2. **word_problem の asked に `draw_graph` を入れた。**
   「関係をグラフに表してから値を求めよ」という融合の文章題のため。
   frame の分担は「文章題か図の問題か」であって「図をかかせるか否か」ではない。
   → **g2_l29 Lv4 はこの資産にそのまま乗る**（残り3セルのうち1つは開通済み）。
3. **動点の経路は A→B→C→D にする。**
   A→B→C（台帳 example の経路）だと面積が「増加→一定」の2区間しかなく、
   指定の面積になる時刻が必ず1つに潰れる＝場合分けが答えに効かない退化になる。
4. **台帳 example の数値が場面として成立していないことがある。**
   g2_l30 Lv4 の例は「2回目の出会い」が相手の到着後に起こる。desc は正として使い、
   数値は成立条件を構成の段階で列挙して引き直す（逸脱は source_desc に明記）。
5. **params 忠実性契約は表示にも効く。** `y = x²` は係数 1 を表示しないので、
   `numbers` に a=1 を置けない。表示に現れない値は候補から外す。

## Phase B：立体の図の描画資産 → C8 + C10 空間（14セル）— ✅ 完了（509 → 523）

**描画資産のボトルネックは「立体を描く資産が無い」こと。**
`graph.py`（座標平面）も `distribution_chart.py`（階級×度数）も 2D の座標系で、
立体の見取図・投影図・展開図はどちらにも乗らない。

### 投資（1回）— ✅ 完了（`fef281f`）

`visuals/solid.py` を新規に起こした。5つの view を1つの visual（`math.solid`）に収める:
| view | 中身 |
|---|---|
| `sketch` | 見取図。斜投影（奥行き右上45°・0.5倍）＋見えない稜線の破線。角柱/角錐/円柱/円錐/球 |
| `projection` | 投影図。立面図（上）と平面図（下）を並べ、対応を破線で結ぶ |
| `net` | 展開図。角柱＝側面の帯＋底面2枚／円柱＝長方形＋円2枚／円錐＝おうぎ形＋円 |
| `section` | 断面。頂点名つきの多角形1枚 |
| `rotation_source` | 回転体の元図。回転させる平面図形＋回転の軸（一点鎖線） |

**調査で分かったこと: 3D の見取図が要るのは14セル中4セルだけ**で、残りは
投影図・展開図・断面・回転前の平面図＝すべて2D図形だった。

**目視で1件バグを見つけた**（展開図の底面を一律に長方形で描いていたため三角柱の
展開図が誤りになる）。図は SVG を PNG に起こして必ず目で見ること
（`rsvg-convert` が使える。ゲートは図の**内容の誤り**を検出しない）。

### 回収

| 束 | セル |
|---|---|
| g1_l49 回転体 graph_table Lv1/2/3 | ✅ `46129c5` |
| g1_l50 投影図 graph_table Lv1/2/3 | ✅ `7059bdf` |
| g1_l51 展開図 graph_table Lv2/3 | ✅ `595a9da` |
| g3_l55 空間図形への三平方 graph_table Lv2 + word_problem Lv4 | ✅ `#130` |
| g3_l56 立体の表面上の最短距離 graph_table Lv2 + word_problem Lv4 | ✅ `#130` |
| g1_l53 球の表面積と体積 find_value Lv1/2 | ✅ `9c1a522` |

**計14セル・全着地。** g1_l53 は
**params が半径ひとつだと dup が原理的に通らない**セルで、軸を3つ
（半径・半径か直径か・単位）にして解決した。「与え方」を足しても op 列が
変わらないよう、solver の第1手を variant 中立にしてある。

**C8 空間図形が 24/24 で完成**し、C10 も 50/51 になった。

### この束で確定した設計判断

1. **図は SVG を PNG に起こして必ず目視する**（`rsvg-convert -o x.png x.svg`）。
   check_cell もゲートも**図の内容の誤り**を検出しない。実際にこの束だけで
   5件（問題図が空・断面が常に長方形・正五/六角柱の底面が正方形・おうぎ形の重なり・
   寸法ラベルの欠落）を目視で見つけた。
2. **対応表を2か所に持たない。** 立体→(立面図,平面図) の表を solver と描画側の
   両方が持っていて食い違った。solver 側を唯一の出典にし、テストで一致を固定する。
3. **frame の禁止要素は「答えの図」を指す。** 与えられた見取図は "solid_given" で
   宣言して問題図に出してよい（read_box_plot が読む対象を描いてよいのと同じ）。
4. **設問が「図中に書き入れよ」なら模範解答図に寸法を書く。** features に値が
   あっても図に無ければ設問に答えていない。

## Phase C：統計の word_problem（5セル）— ✅ 完了（523 → 528・`#131`）

| セル | 内容 |
|---|---|
| g1_l58 Lv2 | 平均値 → 範囲 → どちらが安定しているか（誘導あり3小問） |
| g1_l58 Lv3 | どの代表値で比べても同じ結論＝指標を自分で選べる |
| g1_l58 Lv4 | 探究の進め方を3つから選ぶ |
| g3_l57 Lv2 | 全数調査か標本調査かを判断する（既存 solver をそのまま再利用） |
| g3_l58 Lv2 | 偏りの生じにくい抽出方法を選ぶ |

### この束で確定した設計判断

1. **「自分で選べ」は、どれを選んでも同じ結論になるデータを構成して成立させる。**
   指標を engine が固定すると設問が変わる。平均値・中央値・最頻値がすべて同じ側を
   指すデータだけを作れば、「自分で選んでよい」がそのまま成り立ち答えは一意になる。
2. **判断を問うセルは params が「場面の種類」だけになり dup が通らない**
   （実測 0.90〜0.98）。答えの根拠を変えない数値（母集団の大きさ）を第2軸に足す。
3. **checker は mode を params から読む。** params の形（どのキーがあるか）から
   セルを推測すると、似た形のセルが増えたときに黙って別の solver を呼ぶ（実際に踏んだ）。
4. **本文に出る数はすべて `numbers` に置く。** データ列を別キーに逃がして `numbers` を
   空にしたら、契約テスト（numbers が空なら本文に算用数字が無いこと）に落ちた。
   列は `numbers` に載せ、checker はそこから組み直す＝二重管理にしない。

## Phase D：C13 exam 融合（31セル）★セル数では最大 — **完了（2026-08-08）**

`exam_l1`〜`exam_l7`。内訳は graph_table 7・word_problem 14・find_value 10
（残り1つ exam_l6 の proof は Phase F）。

**着地済み（26セル）**: exam_l1（一次関数と図形・5）・exam_l2（放物線と直線・5）・
exam_l3（動点と面積変化・6）・exam_l5（確率・2）・exam_l6（相似と面積比・4）・
exam_l7（データの活用・4）。いずれも既存単元の recipe モジュールに同居させた
（exam_l3 は motion.py、exam_l5 は word_problem_probability.py、exam_l6 は
similarity_scale_ratio.py）。新しい数学は exam_l5 の余事象2本と exam_l6 の
台形の対角線1本だけで、残りは既存 solver の合成。

**exam_l4（5セル・最後の束）**: 既存の `word_problem_pythagorean.py` に
scenario_kind を3つ足しただけで、find_value/word_problem の4セルが載った。
唯一図が要る graph_table Lv2 は、Phase B の `visuals/solid.py` の断面ビュー
（長方形＋対角線）に `mode="cube_section"` を足すだけで足りた
——**図の資産の投資は Phase B で先に済んでいた**、というのが結果。
**既存単元 solver の合成**（`packs/math/recipes/exam_fusion.py` が既にある）ので
新しい数学はほぼ不要。ただし Lv3/Lv4 のみ＝多段構成が要る。
**Phase A・B が終わっているほど安くなる**（exam_l3 は動点、exam_l4 は空間図形、
exam_l7 は箱ひげ図＝Phase 4 で済）。だから A・B の後に置く。

## Phase E：端物（11セル）— **2/11 着地（2026-08-08）**

単発で束ねようがないもの。並列体の余りに配る。

**着地済み（新 solver 1本だけ）**
- `g3_l53.word_problem Lv4`: 3辺のわかる三角形で補助線を自分で引き高さと面積。
  既存 `word_problem_pythagorean.py` に scenario_kind を足しただけ（新 solver ゼロ）。
  高さが整数に落ちる組（3〜30 で16通り）だけを候補にした。
- `g2_l33.find_value Lv2`: ブーメラン型（三角形の内部の点がつくる角）。
  新設 solver `math.arrowhead_angle`。**図を使わずに成立させた**——どの点をどう
  結ぶかを文で述べれば図が一意に決まるため。

**残り9セル**
- graph_table 7: g1_l37 Lv1・g1_l39 Lv3・g2_l32 Lv1・g2_l44 Lv1・g2_l52 Lv1・
  g2_l53 Lv1・g3_l54 Lv2
  - **図の資産がそのまま無いものが多い**のがこの7セルの本質。
    `g2_l52/l53 Lv1` は**樹形図**を描く新資産が要る（2セルで1投資）。
    `g1_l39 Lv3`（回転の中心を作図で求める）は実質 construction 相当で、
    Phase G の作図資産と一緒に見るのが筋。
    `g3_l54 Lv2`（座標平面上の2点間の距離）は Phase 1 のグラフ資産が効く。
    `g1_l37 Lv1`・`g2_l32 Lv1`・`g2_l44 Lv1` は 2D 幾何の既存描画に載る見込み。
- find_value 1: g2_l50 Lv3（四角形の面積を2等分する直線と辺 BC の交点）
- knowledge 1: g2_l38 Lv2（逆の判別と反例の提示）
  - **命題のカタログだけでは dup が通らない**（数個の命題では組合せ数が足りない）。
    数値をパラメータに持つ命題テンプレート（「n が 2k の倍数ならば k の倍数」等）に
    して変種を作る必要がある。

## Phase F：C15 proof（51セル）★最大の投資

form ごと未実装。frame・solver・checker・テンプレートを新規に設計する必要がある。

設計の勘所（未検討・着手時に詰める）:
- 「証明を書かせる」ではなく**証明の骨格を穴埋めさせる**形式に落とす
  （仮定・使う定理・結論の対応づけ）ことで T1 決定論に収まるか
- 合同条件・相似条件・円周角の3系統でテンプレート化できるか
- 答えの照合は「文字列一致」ではなく**根拠の集合一致**にできるか

| 塊 | セル |
|---|---|
| g2_l38〜l50（合同・二等辺三角形・平行四辺形・等積変形） | 29 |
| g3_l41〜l52（相似条件・平行線と線分の比・中点連結・円周角・三平方の証明） | 13 |
| g2_l7/l8・g3_l13（文字式による数の性質の説明） | 6 |
| g2_l32（平行線の性質と平行になるための条件） | 2 |
| exam_l6 | 1 |

**g2_l38〜l50 の29セルが最大の塊**で、合同条件・二等辺三角形・平行四辺形という
3系統に分かれる。ここをテンプレート化できるかが proof 全体の成否を決める。

## Phase G：C16 construction（9セル）

g1_l41〜l44（垂直二等分線・角の二等分線・垂線・作図の利用）。M2 送り扱い。最後。

---

## 進め方の運用ルール（踏んだ事故の再発防止）

1. **worktree の基点確認を必ず最初にやらせる**
   （過去に旧 master 基点で作られる事故が複数回。ずれていたら `merge --ff-only` で揃える）
2. **`spec_cli check` の `dup_rate` は常に 0.0 で当てにならない**
   → `scratchpad/check_cell.py` の 100seed 実測が本物
3. **ゲートは答えが 0 や 1 に潰れる退化を素通りする**
   → property テストで固定する
4. **eval は「テスト緑」とは別に必ず回す**
   （fingerprint が op 列なので Lv 間で op 列が同じだと level_sep が黙って壊れる）
5. **check_cell が ALL_OK でも pytest 全走は別に必要**（2026-08-07 に2件踏んだ）
   - `frames.py` の `asked_vocab` を増やしたら `test_frames.py` の台帳 frozenset にも足す
   - word_problem は `params["numbers"]` 必須・値はすべて本文に現れること
     （図が数を与えるセルは `"numbers": {}` と明示。空なら本文に算用数字が無いことを
     併せて検査する）
6. **level_sep は「何を文字に置くか」を動かすのが一番効く**
7. **params は「本文に出ている数値」だけ**（導出値を置くと検証に穴があく）
8. **並列度は3〜4体**。競合面は `concepts.yaml` と各 `__init__.py` のみ（追記）なので
   順次マージすれば衝突は軽微。**ただし `test_recipes.py` は両側が末尾に追記するため
   必ず衝突する**。`git show :2:<path>` / `:3:<path>` を取り出し、
   ours + theirs の追記ぶんを末尾に連結して解決する（素朴な手作業は他方のブロックを
   別のテスト関数の中に差し込む事故になる）
9. **サブエージェントは利用上限で落ちうる**。成果物は worktree に残るので、
   次のセッションが `git -C .claude/worktrees/agent-* status` で拾って検証・マージする。
   **落ちた体は DoD の途中で止まっている前提で、check_cell から全部やり直す。**
10. engine 検証は `--no-cov` 必須。フルスイートは約28-33分（47,621テスト）

## 見込み

| Phase | 内容 | セル | 累計 |
|---|---|---|---|
| — | 現在（2026-08-08 実測・Phase D 完了 / Phase E 2/11） | — | **561** |
| E | 端物の残り | 9 | 570 |
| F | proof（exam_l6.proof Lv3 を含む） | 51 | 621 |
| G | construction | 9 | **630** |

Phase E を終えれば **570/630（90.5%）**。残りは proof と construction という
「form ごと新規設計」の2つだけになる。
