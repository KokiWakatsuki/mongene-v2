family: math.g3_l26.knowledge
form: knowledge
source_desc: |
  解の公式の導出と利用 — 与式とa,b,cの対応を識別
  （units.generated.yaml の desc/example を正として転記）。
  Lv1 desc: 与式とa,b,cの対応を識別。
       example: 2次方程式 3x²-2x-5=0 の a、b、c の値をそれぞれ答えよ。
  【設計モデル】用語想起ハブ（既存 math.term_recall・_TERM_MAPS に domain
  "quadratic_coefficient" を追加・concept ごとに答えが "a"/"b"/"c" の1文字＝digit-free）。
  具体例の ax²+bx+c=0（a,b,c すべて非0）を surface に埋め込み、x²/x/定数項のどの位置かを
  説明し、対応する文字を答えさせる。既存ハブへの追加のみ＝既存 golden 不変。
concepts_default: [quadratic_coefficient.term_recall]
levels:
  "1":
    signature: quadratic_coefficient_term_recall
    recipe: math.term_recall
    params:
      domain: quadratic_coefficient
      concept_set: [coeff_a, coeff_b, coeff_c]
      a_domain: {int_range: [-9, 9]}
      number_domain: {int_range: [-30, 30]}
    given: [statement]
    asked: [choice]
    visual: none
    text: {tier: T1, template: lf_term_recall_v1}
    hints: [steps_prefix]
    concept_tags: [quadratic_coefficient.term_recall]
    cause_tags: []
remedial:
  default_level: 1
