family: math.g1_l38.knowledge
form: knowledge
source_desc: |
  平行移動の意味と性質(向き・大きさ不変)を判別する
  （units.generated.yaml の desc/example を正として転記）。
  Lv1 desc: 平行移動の意味と性質(向き・大きさ不変)を判別する。
       example: 図形を平行移動しても変わらないものを、次から2つ選べ。
                ア 図形の向き イ 図形の大きさ ウ 各点の位置 エ 辺の長さ。
  【設計モデル】新設 solver math.judge_transformation_invariant（topic="parallel_translation"
  固定・平行移動しても対応する辺の長さ/図形の大きさは変わらないという恒真の性質を判別。
  具体的な移動量(右へm、上へn)は surface で dup 分散、性質そのものには無関係）。
concepts_default: [transformation_invariant.parallel_translation]
levels:
  "1":
    signature: judge_transformation_invariant_parallel
    recipe: math.judge_transformation_invariant
    params:
      topic: parallel_translation
      # 方眼の目盛りは1〜10（13目盛りの平行移動は方眼に収まらない）。
      distance_domain: {int_range: [1, 10]}
    given: [statement]
    asked: [choice]
    visual: none
    text: {tier: T1, template: lf_judge_transformation_invariant_v1}
    hints: [steps_prefix]
    concept_tags: [transformation_invariant.parallel_translation]
    cause_tags: []
remedial:
  default_level: 1
