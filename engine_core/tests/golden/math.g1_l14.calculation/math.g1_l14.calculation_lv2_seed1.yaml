meta:
  cause_tags: []
  concept_tags:
  - notation.quotient_mixed
  provenance:
    git_commit: ''
    recipe: math.compute_notation
    recipe_version: v1
    spec_version: v1
  purpose: base
  render_keys:
    template: lf_notation_quotient_v1
    tier: T1
  requested:
    form: calculation
    level: 2
    subject: math
    unit: g1_l14
  resolved:
    form: calculation
    level: 2
    subject: math
    unit: g1_l14
  seed: 1
  signature: notation_quotient_mixed
problem_ref: 0cb03ee41b60383e
problem_text: '次の式を、除法の表し方のきまりに従って分数の形で表せ。

  d × x × 2 ÷ m'
sub_questions:
- answer:
    display: 2dx/m
    kind: symbolic
    srepr: Mul(Integer(2), Symbol('d'), Pow(Symbol('m'), Integer(-1)), Symbol('x'))
  cause_tags: []
  concept_tags:
  - notation.quotient_mixed
  explanation: 'まず、乗法の部分を先にまとめてから、分数の形に表す。（2dx）

    次に、除法の記号 ÷ を使わず、分数の形で表す。（2dx/m）'
  hints:
  - 乗法の部分を先にまとめてから、分数の形に表す。
  label: (1)
  prompt_text: 式を簡単にしなさい。
  solution_steps:
  - args: []
    narration: 乗法の部分を先にまとめてから、分数の形に表す。
    op: collect_numerator
    result_display: 2dx
    result_srepr: Mul(Integer(2), Symbol('d'), Pow(Symbol('m'), Integer(-1)), Symbol('x'))
  - args: []
    narration: 除法の記号 ÷ を使わず、分数の形で表す。
    op: rewrite_as_fraction
    result_display: 2dx/m
    result_srepr: Mul(Integer(2), Symbol('d'), Pow(Symbol('m'), Integer(-1)), Symbol('x'))
visual_svg: null
