meta:
  cause_tags: []
  concept_tags:
  - polynomial.multiply_monomials_powers
  provenance:
    git_commit: ''
    recipe: math.compute_monomial_expression
    recipe_version: v1
    spec_version: v1
  purpose: base
  render_keys:
    template: lf_add_subtract_polynomials_v1
    tier: T1
  requested:
    form: calculation
    level: 2
    subject: math
    unit: g2_l4
  resolved:
    form: calculation
    level: 2
    subject: math
    unit: g2_l4
  seed: 2
  signature: multiply_monomials_powers
problem_ref: 632608eb16fa8ba5
problem_text: '次の計算をせよ。

  5a × (-2ab)'
sub_questions:
- answer:
    display: -10a²b
    kind: symbolic
    srepr: Mul(Integer(-1), Integer(10), Pow(Symbol('a'), Integer(2)), Symbol('b'))
  cause_tags: []
  concept_tags:
  - polynomial.multiply_monomials_powers
  explanation: 'まず、かけ合わせる式の符号から、答えの符号を先に決める。（答えの符号を先に決める）

    次に、係数どうしをかけ算する。（係数どうしをかける）

    最後に、同じ文字は指数の和にまとめ、1つの単項式にする。（-10a²b）'
  hints:
  - かけ合わせる式の符号から、答えの符号を先に決める。
  - 係数どうしをかけ算する。
  label: (1)
  prompt_text: simplified_expr を求めなさい。
  solution_steps:
  - args: []
    narration: かけ合わせる式の符号から、答えの符号を先に決める。
    op: determine_sign
    result_display: 答えの符号を先に決める
    result_srepr: Mul(Integer(-1), Integer(10), Pow(Symbol('a'), Integer(2)), Symbol('b'))
  - args: []
    narration: 係数どうしをかけ算する。
    op: multiply_coefficients
    result_display: 係数どうしをかける
    result_srepr: Mul(Integer(-1), Integer(10), Pow(Symbol('a'), Integer(2)), Symbol('b'))
  - args: []
    narration: 同じ文字は指数の和にまとめ、1つの単項式にする。
    op: combine_powers
    result_display: -10a²b
    result_srepr: Mul(Integer(-1), Integer(10), Pow(Symbol('a'), Integer(2)), Symbol('b'))
visual_svg: null
