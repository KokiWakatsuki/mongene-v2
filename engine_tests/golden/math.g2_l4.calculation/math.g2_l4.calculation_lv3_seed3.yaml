meta:
  cause_tags: []
  concept_tags:
  - polynomial.monomial_mul_div_chain
  provenance:
    git_commit: ''
    recipe: math.compute_monomial_expression
    recipe_version: v1
    spec_version: v1
  purpose: base
  render_keys:
    template: lf_add_subtract_polynomials_v1
    tier: T1
  requested:
    form: calculation
    level: 3
    subject: math
    unit: g2_l4
  resolved:
    form: calculation
    level: 3
    subject: math
    unit: g2_l4
  seed: 3
  signature: monomial_mul_div_chain
problem_ref: 506a1d740cea4fdf
problem_text: '次の計算をせよ。

  -4x³y² ÷ (2/3 x²y) × (-4x²y)'
sub_questions:
- answer:
    display: 24x³y²
    kind: symbolic
    srepr: Mul(Integer(24), Pow(Symbol('x'), Integer(3)), Pow(Symbol('y'), Integer(2)))
  cause_tags: []
  concept_tags:
  - polynomial.monomial_mul_div_chain
  explanation: 'まず、÷ を、その式の逆数をかけるかけ算に直す。（(-4x³y²) × (3/(2x²y)) × (-4x²y)）

    次に、係数どうしをかけ算する。（24）

    最後に、同じ文字は指数の和にまとめ、1つの単項式にする。（24x³y²）'
  hints:
  - ÷ を、その式の逆数をかけるかけ算に直す。
  - 係数どうしをかけ算する。
  label: (1)
  prompt_text: 式を簡単にしなさい。
  solution_steps:
  - args: []
    narration: ÷ を、その式の逆数をかけるかけ算に直す。
    op: convert_divisions_to_reciprocal
    result_display: (-4x³y²) × (3/(2x²y)) × (-4x²y)
    result_srepr: Mul(Integer(24), Pow(Symbol('x'), Integer(3)), Pow(Symbol('y'), Integer(2)))
  - args: []
    narration: 係数どうしをかけ算する。
    op: multiply_coefficients
    result_display: '24'
    result_srepr: Mul(Integer(24), Pow(Symbol('x'), Integer(3)), Pow(Symbol('y'), Integer(2)))
  - args: []
    narration: 同じ文字は指数の和にまとめ、1つの単項式にする。
    op: combine_powers
    result_display: 24x³y²
    result_srepr: Mul(Integer(24), Pow(Symbol('x'), Integer(3)), Pow(Symbol('y'), Integer(2)))
visual_svg: null
