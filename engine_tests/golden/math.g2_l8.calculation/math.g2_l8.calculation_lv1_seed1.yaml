meta:
  cause_tags: []
  concept_tags:
  - polynomial.two_digit_number_property
  provenance:
    git_commit: ''
    recipe: math.combine_digit_number
    recipe_version: v1
    spec_version: v1
  purpose: base
  render_keys:
    template: lf_digit_number_v1
    tier: T1
  requested:
    form: calculation
    level: 1
    subject: math
    unit: g2_l8
  resolved:
    form: calculation
    level: 1
    subject: math
    unit: g2_l8
  seed: 1
  signature: two_digit_number_property
problem_ref: 86df07b0bc938747
problem_text: 十の位が d、一の位が t の2けたの自然数 10d+t と、位を入れかえた数 10t+d の差 を計算し、式を簡単にせよ。
sub_questions:
- answer:
    display: 9d - 9t
    kind: symbolic
    srepr: Add(Mul(Integer(9), Symbol('d')), Mul(Integer(-1), Integer(9), Symbol('t')))
  cause_tags: []
  concept_tags:
  - polynomial.two_digit_number_property
  explanation: 'まず、もとの数と、位を入れかえた数を、それぞれ文字式で書き出す。（もとの数と入れかえた数を文字式で書き出す）

    次に、2つの数の差を計算し、同類項をまとめて1つの式にする。（9d - 9t）'
  hints:
  - もとの数と、位を入れかえた数を、それぞれ文字式で書き出す。
  label: (1)
  prompt_text: simplified_expr を求めなさい。
  solution_steps:
  - args: []
    narration: もとの数と、位を入れかえた数を、それぞれ文字式で書き出す。
    op: express_swapped_number
    result_display: もとの数と入れかえた数を文字式で書き出す
    result_srepr: Add(Mul(Integer(9), Symbol('d')), Mul(Integer(-1), Integer(9), Symbol('t')))
  - args: []
    narration: 2つの数の差を計算し、同類項をまとめて1つの式にする。
    op: combine_like_terms
    result_display: 9d - 9t
    result_srepr: Add(Mul(Integer(9), Symbol('d')), Mul(Integer(-1), Integer(9), Symbol('t')))
visual_svg: null
