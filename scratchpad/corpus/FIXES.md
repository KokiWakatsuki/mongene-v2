# 修正の記録（進行中）

`EVALUATION.md` に記録した欠陥を直した記録。**直した順に足す。**
各項目に「何を直したか」「どう確かめたか」を書く。

## 直し方の原則

評価で分かった構図は「**数を現実に戻すと `dup_rate ≤ 0.20` を通せない**」だった。
これに対する手は4つで、**この順に試す**:

0. **定義域ではなく「答えの大きさ」で測る**（2回目のセッションで足した。**まずこれ**）
   — 定義域は広いまま、答えの分母・分子・根号の中に上限を置いて、超えたら組み直す。
   `g1_l7`（累乗）は定義域を狭めたら dup が 0.33 に跳ねたが、この手なら質も dup も通った。
   `g1_l3〜l6`（正負の数の加減）も、定義域を1つも狭めずに分母420→12・dup 0.00 になった。
   **壊れているのは答えの大きさであって、定義域の広さではないことが多い。**
   **上限を足して dup が跳ねたら、上限を緩める前に「引き方が偏っていないか」を見る。**
   `g3_l16` は分母に上限を置いたら dup が 0.30 に跳ねたが、原因は上限ではなく
   「桁数を一様に引いてから桁を引く」ことで、9通りしかない組に確率の 1/9 が集まって
   いたことだった。**通る組を先に全部並べて一様に引く**と 943通りに散り 0.05 になった。
1. **軸を増やす** — 場面・道具・記号を増やして、数を小さく保ったまま組み合わせを稼ぐ
2. **params に記録する** — 場面が params に無いと `dup_key` が話の違いを見落とす
3. **問題空間の狭さを宣言する** — 上の3つで届かないなら、`dup_rate_max` に理由つきで宣言する
   （教材にありうる設定が十数通りしかない単元がある）

**定義域を広げて通すのは、もうしない。狭めるのも最後の手。**

もう1つ、**数が小さいだけでは足りない**。「さいころを800回投げて1の目が756回出た
（相対度数0.945）」は数がどれも小さいが、実験の記録としてありえない。
**場面に対して値がありうるか**は、数の大きさとは別に見る必要がある。

**上限は当てずっぽうで置かない。** 上限を決めたら、**その上限で組が何通り残るかを
先に数える**。100 seed の distinct はおよそ `N(1 − (1 − 1/N)^100)` なので、
`check_cell` を回す前に dup がいくつになるか分かる（dup ≤ 0.20 には約250通り要る）。

**引いたあと、組み上がったものが場面として成り立つかを見る。** answer-first で先に
決めるのは答えであって、場面の整合ではない。長さが負になる・立方体の高さが1辺と違う、
はどちらも「引いたあとを見ていない」ことから出た（S-1 / S-2）。

---

## ✅ D-1 絶対値の二重

`次の数の絶対値を求めよ。|-17/13|` → `次の数の絶対値を求めよ。-17/13`

- `recipes/arithmetic.py` の `disp` から絶対値記号を外した（`expr_str` は `Abs(...)` のままなので計算に影響しない）
- 確認 `g1_l2.calculation.Lv1` rejects=0/120・dup 0.17

## ✅ D-3 語と助詞の間の半角スペース（56セル）

`きょり を何といいますか` → `きょりを何といいますか`

- `templates/letter_expr.py` `templates/polynomial.py` の3つのテンプレートから
  半角スペースを外した（`statement` は体言止めなので空きは不要）
- 確認 全630セルを走査。56セル → **0セル**
  （残った15件は「ア 変化の割合は…」など選択肢記号の後で、これは教科書どおり）

## ✅ D-22 「〜を何というか を何といいますか」の二重

- `recipes/letter_expr.py` の `quadratic_function_terms` の statement を体言止めにした
- 確認 全630セルを走査して0件

## ✅ D-4 「である であることを証明せよ」（6セル）

`四角形ABCD は平行四辺形である であることを証明せよ` → `…平行四辺形であることを証明せよ`

「である」をどちら側が持つかで空きが変わるので、**結論の文を `facts.goal_text` が
言い切りまで作る**ようにした:

```
数式の事実  「AB ＝ CD である」              … 数式と助詞の間は空ける
四角形の種類「四角形ABCD は平行四辺形である」  … 日本語なので詰める
```

テンプレートは `{{ given.conclusion }}ことを証明せよ。` だけを継ぐ。
証明の本文で使う `fact_text` は言い切ったままなので、証明文は変わらない。

- 確認 `g2_l47/l48/l49` `g3_l44`（四角形）と `g2_l40/l41` `g3_l41` `exam_l6`（数式）の
  両方で出力を確認。rejects=0/120

## ✅ D-20 スラッシュが2つ並んで式が読めない

```
y = -62/5/x  →  y = -62/(5x)
y = -4/3x    →  y = (-4/3)x
```

- `solvers/proportion.py` の表示関数で、分数の比例定数を**かっこでまとめる**ようにした
- **recipe 側に同じ表示関数の写しがあり**、solver だけ直しても問題文は変わらなかった。
  recipe が solver の実装を import する形に一本化した
- 確認 `g1_l29` `g1_l32` `g1_l33` `g1_l35` の6セルで rejects=0/120

## ✅ D-13 存在しない道具（28面のさいころ）

```
1個のさいころ（1から28までの目が出る）を3回投げるとき…
→ 袋の中に赤い玉が2個、白い玉が5個入っている。1個取り出してはもとにもどすことを
  4回くり返すとき、少なくとも1回は赤い玉が出る確率を、余事象を利用して求めよ
```

**recipe のコメント自身が理由を書いていた**:

> `# coin は trials しか自由度がなく単独では dup_rate が閾値を超えるため、faces も振れる`

つまり存在しない道具は dup_rate を通すために入れられていた。直し方は原則どおり:

1. 道具を実在するものに戻す（硬貨2面・さいころ6面・くじ・カード・玉）
2. **場面を61通りに増やす**（当たりの本数・はずれの本数・カードの枚数・玉の個数）
3. **場面を params に記録する**（くじ「当たり1・はずれ4」とカード「1〜5」はどちらも
   (5,1) になるので、大きさだけでは別の問題だと分からなかった）

- 試行回数も 3〜12 → 2〜5（台帳の例は「3枚の硬貨」）
- 確認 `g2_l54.find_value.Lv3` rejects=0/120・**dup_rate 0.15**（61場面 × 4回 = 244通り）

## ✅ D-13 多角形（正348角形・内角 4860/29°）— 6セル

```
正348角形の1つの外角の大きさを求めよ        → 正五角形の内角の和を求めよ（540°・108°）
1つの内角が4860/29°である正多角形は…        → 1つの内角が168°である正多角形は…（正30角形）
内角の和が21060°である多角形は…             → 内角の和が1260°である多角形は…（九角形）
```

- `sides_domain` を `{int_range: [3, 400]}` → `{int_set: [3,4,5,6,8,9,10,12,15,18,20,24,30,36]}`
  （360の約数に限ると、1つの内角も外角も整数になる）
- **軸が `sides` しか無いので dup_rate は 0.86 になる。** これは問題空間が
  本質的に14通りしかないため——教材にありうる正多角形はそれだけで、
  それ以上作れば「正348角形」になる
- そこで **`SpecLevel` に `dup_rate_max` を足した**（`dup_rate_reason` の記載が必須。
  理由が無いまま閾値を上げると質の低下が隠れるので、pydantic の検証で落とす）
- 確認 6セルとも rejects=0/120・宣言した上限内で OK

## ✅ D-10 答えが問題文と別の点名（答え側3セル）

```
問題: 三角形ABMで…三角形AKJの面積と、台形KBMJの面積の比を求めよ
答え: 三角形ADE:台形DBCE = 4:21   →   三角形AKJ:台形KBMJ = 4:21

問題: …交点をG、…交点をH、原点をOとする
答え: P(5, 12)、△OPQ = 12         →   G(5, 12)、△OGH = 12

問題: …2点F、Dで交わり…三角形OFDの面積を求めよ
答え: a = 4、△OAB = 60            →   a = 4、△OFD = 60
```

**頂点名は recipe が引いているのに、solver が受け取っていなかった。**
`labels` は既に params にあったので、solver の引数に足して表示文を組み立てさせた
（`pythagoras_proof` が `letters`/`names` を受け取っているのと同じ形）。
checker からも同じ経路で渡す。

- 直した solver: `similarity_scale_ratio` / `exam_linear_figure` / `quadratic_function`
- 確認 `g3_l45.find_value.Lv3` `exam_l1.find_value.Lv3` `exam_l2.find_value.Lv4`
  `exam_l6.word_problem.Lv3` で rejects=0/120・dup 0.00〜0.08

## ✅ D-10 解説が問題文と別の点名（残り。**走査したら9セルではなく12セルだった**）

引き継ぎの一覧は9セルだったが、全630セルを機械で走査すると**12セル**あった
（動点の「点 P」が g3_l38 と exam_l3 にも出ていた）。検査は
`scratchpad/scan_point_names.py` に残した——**答え・ヒント・解説に出る大文字のうち、
問題文に一度も出ないもの**を挙げる。

| 直したもの | 中身 |
|---|---|
| `solvers/motion.py` | 「点 P がどの辺の上にあるか」→ 問題文の点名（5セル） |
| `solvers/parallel_line_ratio.py` | 「DE∥BC」「AD:DBとAE:EC」→ 問題文の記号（3セル） |
| `solvers/letter_expr.py`（`recall_rule_statement`） | **選択肢そのもの**が「ADとABの比」だった（2セル） |
| `solvers/inscribed_angle.py` | 「直線ABの同じ側にある2点C, D」（1セル） |
| `solvers/similarity_conditions.py` | 「AB//CD であることから」（1セル・下の D-25 と同時） |

- 手は既存と同じ: recipe が引いた点名を solver に渡し、無ければ既定を使う。
  選択肢に点名が出る規則（`_LABELED_RULE_TOPICS`）は記述を `{a}`〜`{e}` の穴にした。
- 確認 12セルすべて rejects=0/120・dup 0.00。走査は **16セル → 1セル**（残る1件は
  `g2_l15.calculation.Lv2` の「A=B=C を A=C と B=C に分ける」＝教科書どおりの表記）

## ✅ D-16 作図の答えが、図に無い座標系と中学範囲外の式（作図9セル）

```
答え: 中点(2, -5) / 線分CDの垂直二等分線＝直線 2x - 5y - 29 = 0
  →  線分CDの中点 / 線分CDの垂直二等分線（かいた直線）
解説: 中心 C(0, 0) → 点Cを中心とする弧
```

**直しは表示層だけで閉じた。** 座標と係数は `srepr`（機械の照合用）に残っているので
double-solve の判定は 1 行も変わらない。生徒に見せる `display` から座標と
`ax+by+c=0` を落とし、「何をかいたか」を書くようにした。

- 確認 9セルとも rejects=0/120。**答え・手順・ヒント・解説から数字が0件**になったことを
  機械で確認（座標の形 `(-2, -1/2)` と `= 0` も0件）

## ✅ D-12 数と設定の現実味（主要なもの）

**直し方はすべて原則どおり**——定義域は狭め、失う組み合わせは軸で取り戻し、
それでも足りない1件だけ `dup_rate_max` に理由つきで宣言した。

| セル | 前 | 後 | 稼いだ軸 |
|---|---|---|---|
| `g3_l39/l40/l41/l49.find_value.Lv2` | 辺 1〜200 を独立に引き `34762/37` | 相似比の倍数だけを組む（答えは整数） | 点名を params に記録 |
| `g3_l42.find_value.Lv2` `g3_l43.find_value.Lv3` | `10370/111` `39/2` | `BC=DE(AD+DB)/AD` が整数の組だけ（1124通り） | 点名 |
| `g3_l45/l46.find_value.Lv2` | 相似比 24:41 → 体積比 `13824:68921` | 相似比は7まで | 立体の種類6 × 名前 |
| `g3_l51.find_value.Lv1` `g3_l53.find_value.Lv2` | `√3106` `√2257` | 斜辺が整数か根号の中が30以下 | 言い回し2 × 点名 |
| `g3_l23.find_value.Lv2` | 面積19133 → `19√53` | 面積600まで | 場面6（紙・タイル・花だん…） |
| `g1_l53.find_value.Lv1/2` | 直径94cm → `415292π/3` | 半径15まで | 場面8 × 単位 |
| `g3_l16.calculation.Lv1` | `9/92` → 循環節22桁 | 循環節6桁まで | （組は1096通りあるので不要） |
| `g3_l29.word_problem.Lv2` | 2数の積が `132860` | 小さい方は30まで | 整数・偶数・奇数の3通り＋**`dup_rate_max: 0.60` 宣言** |
| `g3_l30.word_problem.Lv2` | 面積1924cm² | 縦20・差12まで | 題材5 |
| `g3_l30.calculation.Lv2` | `x(x+9)=3402` | 解は24まで | — |
| `g3_l55.find_value.Lv4` `exam_l4.word_problem` | 1辺57cmの正四面体 | 1辺24まで | 点名 |
| `g1_l7.calculation.Lv1/2` | `(-14/3)³ = -2744/27` | **答えの分子・分母**に上限（定義域は広いまま） | — |
| `g2_l53.find_value.Lv2`（D-7） | 51人を樹形図で | 4〜6人 | 場面5 × 名前8 |
| `g2_l54.find_value.Lv3` | `29679/160000` | 分母1296まで | 場面61 → 149 |
| `g2_l51.find_value.Lv2` ほか（D-19/D-24） | 相対度数が `363/646` | **小数**（割り切れる組だけ） | 道具5・回数10 |
| `exam_l5.calculation.Lv2` | 「ある試行で全部で164通り」 | 場面のある文・60通りまで | 場面4 |
| `g1_l55.calculation.Lv1`（D-24） | 相対度数が `24/41` | **小数**（割り切れる組だけ） | 総度数9通り |
| `g3_l17/l18.calculation.Lv2`（D-18） | `√4 ÷ √8` `3√4617` | 平方数を除き、相殺を禁じ、根号の中に上限 | — |

**新しく分かったこと**: 数を小さくするだけでは足りない。
`さいころを800回投げて1の目が756回出た（相対度数0.945）` は数はどれも小さいが、
**場面に対して値がありえない**。道具ごとに起こりうる相対度数の幅を持たせた。

## ✅ D-25（新規）解説に生の sympy が出ていた

```
解説: Eq(5*x + y, 45), Eq(-10*x + y, 0) の連立方程式を立てる
  →  5x + y = 45, -10x + y = 0
```

`sympy.sstr(Eq(...))` をそのまま `result_display` に入れていた。
`solvers/linear.py` に `_format_equation_display` を足した（3セル）。

## ✅ D-25b（新規）AB∥CD なのに「線分ABと線分CDが点Oで交わり」

図として成り立たない文だった（平行な2線分は交わらない）。交わるのは AC と BD。
`g3_l40.find_value.Lv2` の場面文を直した。

## ✅ D-26（新規）小数で与えた累乗の答えが分数

`(-0.9)³ → -729/1000`。中1で小数の累乗の答えを分数で書く教材は無い（`-0.729`）。
底を小数のまま式に載せ、solver が「小数で与えたら小数で答える」を判断できるようにした。

## ✅ D-2 正負の数の分母が最大420（4セル）

```
7/9-(-1.2)+4/7-0.6 → 614/315
(-6/7)+(-3.5)+(-2) → -89/14
```

この単元の狙いは**符号の処理**なのに、通分の負荷が支配していた。

**定義域は1つも狭めていない。** レベルに `answer_denominator_max: 12` を宣言し、
recipe が「答えの分母が上限を超えたら組み直す」ようにした（`g1_l7` の累乗で効いたのと
同じ手＝**壊れているのは答えの大きさであって定義域の広さではない**）。

- 座標 `g1_l3` `g1_l4` `g1_l5` `g1_l6` の `calculation.Lv2`
- 確認 60 seed ずつ実測。**分母13以上 107/240 → 0/240**・最大分母 420 → 12。
  rejects=0/120・**dup_rate は 4セルとも 0.00**（定義域を狭めていないので落ちない）

## ✅ D-17 1手のセルのヒントが中身なし（**25セルではなく26セル**）

```
問題: 次の式を計算せよ。 (√105)²
ヒント: 問題文の与えられた値をもう一度確認しよう。   ← 何も言っていない
  →   根号のついた数を2乗し、根号の中の数にもどす。
```

1手で解ける問題は「最後の手は答えを明かすのでヒントにしない」という規則で
候補が空になり、既定の文に落ちていた。**narration には数字を書かない規約**が
あるので、1手のときはその手の narration をそのまま出してよい——
「使う規則を、答えの値を明かさずに教える」ことになる。

優先順は「手で書かれたテンプレヒント → 1手の narration → 既定」。
（最初 narration を最優先にしたら、テンプレヒントを持つセルのテストが落ちた。
テストのほうが正しい——手で書いたヒントがあるならそれがいちばん良い。）

- 検査 `scratchpad/scan_empty_hints.py`（**26セル → 0セル**）
- 確認 26セルすべて rejects=0/120（narration が hints に流れても G-Q5t は通る）

## ✅ D-9 正解が複数あるのに答えを1つしか持たない（2セル）

```
問題: …格子点を、原点以外に一つ読み取り、その座標を答えよ   → 答え (4, 14)
  →  …格子点のうち、x座標が4である点の座標を答えよ
```

「一つ読み取り」と言いながら答えを1つしか持てず、正解のはずの (2, 7) を書いた
生徒が不正解になっていた。**x座標だけを与えて答えを一意にする**（式は与えない
ので、y は図から読むしかない＝題材は壊れない）。`g1_l32` と `g1_l35`。

## ✅ D-8 問題文が答えを言っている（1セル）

このセルは**そもそも「図から読む」問題ではなかった**（座標は本文が与え、図は
空の方眼で、生徒はそこに点をとる）。それなのに解説が「どれだけ進んだ位置かを
読み」と言っていたので、読む対象が無いのに読めと指示していた。
解説を実際にやること（受け取って、並べて、方眼の上でその位置に点をとる）に直した。

## ✅ D-6 「右の図で」と書いてあるのに図が無い（2セル）

どちらも `visual: none`。**配置は文が言い切っている**（「それぞれ点A, K, Rで
交わり」「4点がこの順に円周上にある」）ので、図への言及だけを外した。
`g3_l42.find_value.Lv3` は答えも `105/19` だったので、整数になる組に直した。

## ✅ D-5 数直線の答えが仮分数 / D-21 Lv1 の答えが Lv2 の形式 / D-23 円周角が分数

| | 前 | 後 |
|---|---|---|
| D-5 `g1_l2.graph_table.Lv1` | `-17/2` | `-8.5`（小数で書き切れるときは小数） |
| D-21 `g3_l15.calculation.Lv1` | `2√11 < √82` | `√44 < √82`（**与式の形のまま並べる**） |
| D-23 `g3_l47.find_value.Lv1` | 中心角9° → `9/2°` | 中心角は偶数だけ（円周角は整数） |

D-21 は sympy の自動簡約が原因。`fmt_radical_source`（与式の文字列を整形する関数）を
足して、簡約せずに問題文の形のまま出す。

## ✅ D-14 統計のデータが機械的（2セル）

```
A: 4、8、4、8、5、7、4、8、5、7、5、7   ← 平均に合わせた対が丸見え
A組: 91、91、91、91、91、91、91、91…   ← 100m走で8人が同タイム・9.1秒
```

- **対で並べるのをやめた**（撹拌する。平均も範囲も並び順では変わらない）
- **同じ値の重なりを減らした**（最頻値が一意なだけの数に。8個 → 4個）
- **種目ごとに、実際にありうる値の幅を持たせた**（`_TIMED_EVENTS`）。
  1つの `base_range` を全種目で使い回していたので 100m走が9.1秒になっていた
- 「立ち幅とびの助走時間」は立ち幅とびに助走が無いので外した
- 「一週間の読書時間」なのに値が12個 → 「週ごとの読書時間」

## ✅ D-11 図の PNG に背景が無い / D-15 `ℓ` が豆腐になる

- 背景の矩形を `fill="none"` → `fill="#ffffff"`（9か所）。
  PNG に起こして左上が白・線が見えることを実測
- SVG のルートに `font-family` を指定（9か所）。既定フォントには `ℓ`（U+2113）が
  無く □ になっていた。指定すると正しい筆記体の ℓ が出る（PNG に起こして目視）

## ✅ D-27（新規）座標軸の目盛りラベルが重なって読めない

`-16-15-14-13-12-11-10-9-8…` とつながっていた。隣が 20px 未満なら 2目盛おき・
5目盛おきに間引く。**whitelist と実描画の両方を同じ関数から作る**
（片方だけ直すと G-Q5v が落ちる）。図を持つ130セルで拒否ゼロを実測。

## ✅ D-28（新規）コーパスを作り直したら、同種の取りこぼしが7件出た

**これがこのセッションでいちばん重要な発見。** 直したつもりの欠陥が、
同じ単元の別のセルに残っていた。1セルずつ直していると、同じ根の別の出口を見落とす。
**コーパスを作り直して読み直すまで見えなかった。**

| 直したもの | 中身 |
|---|---|
| さいころの面数 4セル | 20面・17面・14面・9面 → **6面**（D-13 の取りこぼし） |
| カードの枚数 2セル | 53枚・80枚 → 5〜20枚 |
| 相対度数 3セル | `27/40` `7/36` `9/20` → **小数**（D-24 の取りこぼし） |
| 平均値 1セル | `694/23` → 小数で書き切れる度数だけを引く |
| 反比例の比例定数 | `-62/5` → 整数（教科書は `y = 12/x`） |
| 絶対値で与える数 | `-17/13` → 分母6まで（教科書は `-5, -2.5, -1/2`） |

さいころを6面に戻すと dup が 0.94 まで跳ねる。原則どおり順に当てた:

1. **問い方の種類を増やした** — 和・積・差・倍数・少なくとも一方（8種類）。
   solver に条件を5つ足した。これは教科書にある問い方なので、質も上がる
2. **道具を増やした** — 「少なくとも1回」はカード・玉でも作れる（`_REPEAT_TOOLS`）
3. **params に記録した** — 余事象は条件が違っても確率が同じなら同じ値になるので、
   `condition` と `target` を params に入れないと dup_key が話の違いを見落とす
4. **残りは宣言した** — 5セルに `dup_rate_max`（0.35〜0.80）を理由つきで

> 大小2個のさいころ（6面）で問える条件は、和・積・差・倍数・少なくとも一方を
> 合わせても57通りしかない。既定の0.20を通すには面数を14面・20面まで広げる
> ことになり、存在しないさいころになる。

### **2周目でも4件出た**（＝1周では終わらない）

D-28 を直したあと、もう一度コーパスを作り直して走査したら、また4件出た:

| セル | 中身 |
|---|---|
| `g2_l51.knowledge.Lv2` | **1から87までのカード**（「同様に確からしい」の判定） |
| `g2_l51.word_problem.Lv3` | 21枚のカード（上限20に揃えた） |
| `g3_l59.word_problem.Lv2` | 標本での割合が `1/8` |
| `g3_l60.word_problem.Lv3` | 標本での割合が `1/12` |

**「作り直す → 走査 → 直す」は1周では終わらない。** 走査が空になるまで回すこと。
2周目の立て直しも原則どおり——「同様に確からしい」の場面を6→16に増やして
dup 0.35 → 0.12（枚数を広げるのではなく場面を増やした）。

再発検査を `scratchpad/scan_defects.py` に2つ足した:
`存在しない面数の道具` と `相対度数・割合を分数で答えている`（この2つが11件のうち9件を釣った）。

---

# 3周目（2026-08-10・別セッション）

`engine.eval` を回して**4ゲート全通過（exit 0）**を確認したうえで、コーパスを3周目まで
作り直して走査し、未精読だった3か所（中3 calculation・中2 knowledge・word_problem 全100
セル）を精読した。**走査で5件、精読で3件**出た。どれも既存の欠陥と同じ根の別の出口。

## ✅ R-1 平均値が仮分数（`g1_l57.calculation.Lv1`）

`平均値 271/8、中央値 35、最頻値 35`。D-28 で「平均値 `694/23`」を直したのは Lv2 で、
**Lv1 が残っていた**。

- 値を独立に引いていたので合計が個数で割り切れなかった。**最後の1つだけを、平均が
  割り切れる値から引く**ようにした（定義域 `value_domain` は狭めていない）
- 併せて solver の表示に `_fmt_relative` を通した（中央値も個数が偶数だと `71/2` になる）
- 確認 rejects=0/120・dup 0.00

## ✅ R-2 円周角が分数（`g3_l50.find_value.Lv3`）

`円周を8等分する点…∠HJE` → `225/2°`。D-23（円周角が分数）の取りこぼし。

- 円周角は `span × 180/n`。**整数になる組だけ**を引くようにした。n=7 はどの span でも
  整数にならないので、この条件だけで自然に落ちる（`n_domain` は `[5,9]` のまま）
- 確認 rejects=0/120・dup 0.00

## ✅ R-3 循環小数→分数が `821/1980`（`g3_l16.calculation.Lv2`）

D-12 で Lv1（循環節22桁）を直したときに Lv2 を見ていなかった。

**ここで分かったことが、この周でいちばん大事**:

1. まず原則⓪どおり「答えの分母 ≤ 99」を足した → **dup が 0.30 に跳ねた**
2. 跳ねた原因は上限ではなく**引き方**だった。桁数 (m,n) を一様に引いてから桁を引いて
   いたので、**9通りしかない (m,n)=(0,1) に確率の 1/9 が集まっていた**
3. **通る組を先に全部並べて、そこから一様に引く**ようにしたら 943通りに散り、
   dup 0.05 になった

> **原則⓪（答えの大きさで測る）と原則①（軸を増やす）は両立する。**
> 上限を足して dup が跳ねたら、上限を緩める前に「引き方が偏っていないか」を見る。

- 確認 rejects=0/120・dup **0.30 → 0.05**

## ✅ R-4 「差 を計算し」の半角スペース（`g2_l8.calculation.Lv1`）

`templates/polynomial.py` の `LF_DIGIT_NUMBER_V1`。D-3 の取りこぼし。
スロットが「…の和」「…の差」と**日本語の名詞で終わる**ので空きは要らない
（式で終わる `LF_NUMBER_PROPERTY_V1` とは違う——理由をコメントに残した）。

## ✅ R-5 カードの枚数の上限（`g2_l51.word_problem.Lv3`）

走査は「1から21までのカード」を当てたが、**これは偽陽性**と判断した。
倍数の包除（4の倍数または5の倍数）は `lcm(a,b)` 以上の枚数がないと重なりが実在せず、
教科書も「1から30まで」「1から50まで」の形で出す。

- `n_range` は `[20, 60]` → `[20, 50]`（60枚は教材として広い）
- 走査の規則に**理由つきの例外**を1つ足した（倍数を問う場面は50枚まで許す）

## ✅ D-29 標本調査の母集団が場面に対してありえない（`g3_l57`/`g3_l58`/`g1_l58`）

```
ある湖にすむ魚のおよその数を調べたい。対象は全部で270匹ある。   ← 数を問うのに数を与えている
ある市の有権者が、選挙でどの候補者に投票したか…対象は全部で240人ある
ある図書館で、蔵書がすべてそろっているかを確かめたい。対象は全部で90冊ある
ある市の家庭のごみの量を…母集団は全部で480人である            ← ごみの母集団は世帯
```

**family YAML のコメント自身が理由を書いていた**（D-13 の「28面のさいころ」と同じ構図）:

> 母集団の大きさ（答えを変えない・**dup の第2軸**）。場面16通り × 46 = 736 通り。
> **場面の種類だけだと16通りしかなく、100seed で dup 0.98 になった**（実測）。

直しは原則①どおり——**幅を場面ごとに持たせる**（D-14 の `_TIMED_EVENTS` と同じ形）。
場面の末尾に `lo,hi,step` を持たせ、どの場面も46通り前後に揃えたので**広さは変わらない**。

| 場面 | 前 | 後 |
|---|---|---|
| 市の有権者 | 60〜510人 | 30000〜120000人 |
| 図書館の蔵書 | 60〜510冊 | 10000〜55000冊 |
| 放送局の視聴 | 60〜510世帯 | 100000〜550000世帯 |
| 部活の部員 | 60〜510人 | 10〜60人 |
| 湖の魚（自己矛盾） | — | 場面ごと差し替え（養殖池のこいの重さ） |

母集団の**単位**も場面ごとにした（ごみの量は世帯）。「あるクラスで全員の希望」は
30〜40人しかとれないので「ある中学校で全校生徒の希望」に直した。

- 確認 rejects=0/120・dup 0.06 / 0.14 / 0.12

## ✅ D-30 確率の用語想起の文が壊れている（`g2_l51.knowledge.Lv1`・`g1_l59.knowledge.Lv1`）

```
390回中の一部で起こることがらのように、あることがらの起こりやすさの程度を表す数
  ← 日本語として意味をなしていない
1から487までの番号のカードのように、…       ← D-13（存在しない道具）と同じ根
```

`number_domain`（10〜500）を3つの concept で共有していたのが原因。
**定義域は狭めず**、引いた n は「回数」（10〜500 はどれも実験としてありうる）に使い、
道具の個数は n から作った小さな数にした。失う組み合わせは道具3通りと2つ目の数で
取り戻す（原則①）。

- 途中で「1から4までのカードから5の倍数を引く」（該当0枚）が出たので、
  **倍数はカードの中に2枚以上あるものだけ**に限った
- 確認 rejects=0/120・dup 0.16 / 0.04

## ✅ D-31 答えの大きさの取りこぼし3件

| セル | 前 | 後 | 手 |
|---|---|---|---|
| `exam_l4.word_problem.Lv4` | 1辺23cm → 体積 `12167√2/12` | 1辺6cm → `18√2` | 体積の係数の分子に上限（`edge_domain` は `[2,24]` のまま） |
| `g1_l15.word_problem.Lv2` | 分速106m → `53a/500km` | 分速125m → `a/8 km` | 答えの分母に上限＋**文字の軸**を足す（`speed_domain` は `[10,300]` のまま） |
| `g3_l29.word_problem.Lv3` | 「5倍より**1554**大きい」 | 「7倍より**126**大きい」 | 本文に出る m に上限＋**言い方3通り**を足す（`small_max` は 60 のまま） |

`g1_l15` では単位の表示も直した（`a/8km` は km が分母に続いて読める＝D-20 と同じ）。

## ✅ 走査の穴（`build_corpus.py` が答えを1つしか出していなかった）

「答えが空 213セル」の正体は**コーパス生成側**だった:

- `res.sub_questions[0]` しか見ていない → (2)(3) の答えを走査が一度も見ていない
- 選択（`ChoiceAnswer`）と作図（`GraphAnswer`）は `display` を持たないので空欄

全小問ぶん・選択肢の正解・作図の特徴点を出すようにした。
**走査の対象が213セル分増えた。**

## ✅ 4周目 — `build_corpus` を直したら、見えていなかった2件が出た

3周目の修正のあとコーパスを作り直すと、「答えが空」が **213セル → 0セル** になった
（`build_corpus` の修正が効いた）。その213セル分の答えが初めて走査に見えた結果、
**新たに2件当たった**。

| | セル | 中身 | 同じ根 |
|---|---|---|---|
| R-6 | `g1_l55.graph_table.Lv2` | 相対度数が `1/6`（総度数36人） | D-24 |
| R-7 | `exam_l7.graph_table.Lv3` | 四分位数が `27/2` `91/2` | D-5 / R-1 |

- **R-6** 総度数を **20・25・40・50**（2と5だけでできた数）に揃えると、どの階級の
  相対度数も必ず割り切れる。**度数の定義域（2〜12）は狭めていない**——最後の1つだけを
  「合計が目標になる値」に決める。表示も小数にした（`0.25` `0.1` `0.275`…）
- **R-7** 整数データの四分位数は必ず整数か x.5 にしかならないので、**表示だけ**を直した
  （`27/2` → `13.5`）。同じ solver を使う `g2_l56.graph_table.Lv3` も一緒に直った
- 確認 4セルとも rejects=0/120・dup 0.00〜0.02

> **走査は、コーパスが印字したものしか見られない。**
> 「作り直す → 走査 → 直す」の輪に、**コーパスが何を出しているか**も入る。

## ✅ 5周目 — 走査は空。読んだら4件出た

5周目の走査は**実物0件**（3周目5件 → 4周目2件 → 5周目0件で収束）。
そのうえで、初めて印字された213セル分の答え（knowledge 208・作図26・グラフ72）を読んだ。
**機械の走査には引っかからない「問いと答えの噛み合わせ」**が4件出た。

| | セル | 中身 |
|---|---|---|
| R-8【致命】 | `g1_l45.knowledge.Lv2` | 「弧の長さは**どうなるか**、答えよ」と問うて答えが「**正しい**」 |
| R-9 | `g1_l56.knowledge.Lv1` | 「**337個の階級**に分けた度数分布表」（`number_domain` 3〜500） |
| R-10 | `g3_l48.knowledge.Lv2` | 円周角が **1°・2°・4°**（`angle_domain` 1〜89）。差1°の判定も |
| R-11 | `g1_l58.graph_table.Lv3` | 階級値が **`25/2kg`** |

- **R-8** 答えは「正しい／誤り」の判別なのに問題文が値を尋ねていた。生徒は「2倍になる」と
  書き、正解と一致しない。**問いの型を答えの型にそろえた**
  （「…弧の長さも2倍になる」といえるか、答えよ）
- **R-9/R-10** 場面としてありうる幅に戻し（階級5〜12個・円周角20〜80°）、
  失う組み合わせは軸で取り戻した（場面8通り・総度数7通り）
- **R-11** 階級値は「下端と上端の平均」なので階級幅が奇数だと x.5 になる。表示だけを直した

### R-9 で踏んだ罠 — **軸を2つ足したつもりが、連動していて1つだった**

階級数と場面をどちらも `n % 8` から作ったので、64通りのはずが**8通りにしかならず**
dup が 0.34 に跳ねた。別の桁（`n // 8`）から作って 0.05 になった。
`g3_l16` の「引き方の偏り」と同じ型——**軸を足したら、実際に何通りになったかを
`check_cell` で測る**。

### 判断して直さなかったもの

| | なぜ |
|---|---|
| `g3_l34.graph_table.Lv2` の変域 `-25/2 ≦ y ≦ 0` | 変域を分数で書く教科書はある。同じ表示関数が作る `傾き -5/4` を小数にすると逆に誤りになる |
| 相対度数 `0.21875`（総度数32） | 細かいが誤りではない |
| 飾りの大きな数（`y = 411x²`・「367より大きい数ならば」・確率 `223/256`） | 答えに効かない飾り。D-12 の裾として前任者が「一律に狭めない」と判断したものを踏襲 |

## ✅ engine_tests が HEAD の時点で1本落ちていた

`test_dup_rate_fails_under_impossible_threshold`。引き継ぎ書の
「49,589 passed / 0 failed」はコミットされた状態には当てはまっていなかった
（**HEAD に worktree を切って同じ1本だけを回して確認**）。

原因は前任者が入れた `dup_rate_max` そのもの。テストは「閾値に -1.0 を渡せば全セルが
不合格」という不変条件を書いていたが、`_threshold_for` は宣言があるセルでは宣言値を
使うので、宣言した13セルは外から渡した閾値では動かない。**設計変更にテストが追随して
いなかった**（E-2 と同じ型）。

テストを設計に合わせた——宣言の無いセルは従来どおり全部不合格、**宣言のあるセルは
宣言値で判定されていること**を確かめる（宣言が黙って効かなくなる回帰をこの1本で止める）。

---

## 付随して必要だった作業

### golden の再承認（279 family・2回）

前回は E-3（問いかけの日本語化）で全セルの `prompt_text` が変わったため。
今回は D-10/D-16/D-12 で**問題文と答えの表示が広く変わった**ため、もう一度全件。

### テストの直し（3件・いずれも「両側結合」）

- `test_recall_rule_double_solve_property`: solver を labels 無しで呼んで recipe の答えと
  比べていた。**checker と同じ経路**（`mr.params["labels"]` を渡す）に直した
- `test_word_problem_quadratic_*`: 連続する2数の差が常に1である前提を持っていた
  （偶数・奇数を足したので差は1か2）
- 「params の数値はすべて given に現れる」検査: 差 `gap` は本文では
  「正の整数／正の偶数／正の奇数」という**語**で示されるので数字としては現れない。
  例外として明記した

---

# 4回目のセッション（2026-08-13）— 未読の form を読んだ

走査は6周目で**実物0件**のまま。そこで、まだ読んでいなかった
**find_value 105セル・proof 51セル・graph_table/construction 全件・
中1 knowledge/calculation・中2 calculation・中3 knowledge**を読んだ。**8件出た**。

proof 51セルは**欠陥なし**（探索と前向き推論で作ったぶん、文面が安定している）。

## ✅ S-1【致命】線分の長さが負（`g3_l43.find_value.Lv2`）

「NG=16cm, GR=1cm, NE=48cm, **EM=-5cm**」。「平行ではない」構成は
比の合う `ec` に `delta`（±1〜8）を足して作るが、**足したあと正かどうかを見ていなかった**。

```python
if ec <= 0 or max(ad, db, ae, ec) > _MAX_SEGMENT_LENGTH:  # 120cm
    continue
```

長さの上限も同時に置いた（`PK=210cm` のような、図に描けない三角形が出ていた）。
実測 **dup 0.00**（定義域は1つも触っていない）。

## ✅ S-2【致命】「1辺が14cmの立方体」の高さが 3cm（`g1_l50.graph_table`）

投影図の recipe は `base` と `height` を**相異になるように**別々に引いていた。
正四角柱と立方体を図で見分けるための規則だが、**立方体と球は1辺（半径）で高さも
決まる**ので、そこに効かせてはいけなかった。

```python
_HEIGHT_FROM_BASE_KINDS = frozenset({"cube", "sphere"})
height = base if kind in _HEIGHT_FROM_BASE_KINDS else <相異に引く>
```

実測 dup Lv1 0.15 / Lv2 0.07 / Lv3 0.13。

> **S-1 と S-2 は同じ形**——引いたあと、**組み上がったものが場面として成り立つか**を
> 見ていない。answer-first で先に決めるのは答えであって、場面の整合ではない。

## ✅ S-3 箱ひげ図の値が場面に合っていない（`g2_l56`/`g2_l57`/`exam_l7`）

「水泳部の自由形の記録（秒）**最小値4・最大値32**」「一日の睡眠時間（時間）
**第一四分位数3**」。数直線の**左端が 0 固定**で、目もり位置だけを 1〜9 から引いていた。

**定義域（目もりの組み合わせ C(9,5)=126通り）は1つも減らさずに直す**——場面が
左端を持つようにした。場面の文字列は元から `"語|…|目もり間隔"` だったので、
間隔のところだけを `"間隔@左端"` に拡張すれば、**どの呼び出し側の分解も変えずに済む**
（場面文字列は3語・4語・6語と長さが違うので、末尾に足す設計は壊れる）。

| 場面 | 前 | 後 | 値の帯 |
|---|---|---|---|
| 自由形の記録（秒） | `4` | `2@12` | 14〜30秒 |
| 一日の睡眠時間（時間） | `1` | `1@3` | 4〜12時間 |
| ハンドボール投げ（メートル） | `4` | `2@8` | 10〜26m |
| 一日に歩いた歩数（百歩） | `5` | `5@20` | 2500〜6500歩 |

実測 dup は 0.00〜0.01（変化なし）。8セルすべて rejects=0/120。

## ✅ S-4【致命】度数分布表が「1区間目…」で問題が解けない（`g1_l56.calculation.Lv2`）

「**8時間未満**の生徒までの累積相対度数を求めよ。〔階級と度数：**1区間目**…18人、
2区間目…4人、…〕」——どの区間が8時間未満なのかが表から読めない。
境界の時刻 `boundary_hour` を、問う階級 `target_index` と**無関係に引いていた**。

階級に実際の時間を書き、境界はその階級の上端として**導く**ようにした:

```python
start_hour = int(draw(p["start_domain"], rng))   # 5〜6
boundary_hour = start_hour + target_index + 1
table_text = "、".join(f"{start_hour+i}時間以上{start_hour+i+1}時間未満…{f}人" ...)
```

実測 dup 0.01。

## ✅ S-5 「係数を何といいますか → b」（`g3_l26.knowledge.Lv1`）— R-8 と同じ根

テンプレは `{{ given.statement }}を何といいますか。` で閉じるので、
statement は**その名前が答えになる語**で終わらなければならない。
「x の項の係数」で終わっていたので、答え `b` と噛み合っていなかった。

> 2次方程式 4x² + 4x − 14 = 0 を一般の形 ax² + bx + c = 0 と見比べたとき、
> x の項の係数**にあたる文字**を何といいますか。

## ✅ S-6 22個（人）の集団に「標本調査」（`g3_l57`/`g3_l58.knowledge.Lv1`）— D-29

`number_domain`（10〜500）をそのまま母集団の大きさに使っていた。
**定義域は触らず、場面が要求する桁だけを上げる**（D-30 の `times = round(n/10)*10` と同じ手）:

```python
def _population_size(n: int) -> int:
    return n * 100        # 1000〜50000
```

全数調査（すべてを調べる調査）は小さい集団のままでよいので、そちらは変えていない。

## ✅ S-7 候補にいると書かれていない人の確率（`g2_l53.find_value.Lv2`）

「6人の委員の候補から…**りくさん**が書記に選ばれる確率」。
「**りくさんをふくむ**6人の委員の候補から」に直した。実測 dup 0.10。

## ✅ S-8 線分ABが `5√442`（`g3_l37`/`exam_l2.find_value.Lv3`）— 原則⓪

AB = |xB − xA|·√(1 + m²)（m = a(xA + xB)）なので、m が大きいと根号の中が跳ねる。
**定義域は広いまま、根号の中に上限を置いて引き直す**:

```python
_MAX_SEGMENT_RADICAND = 65      # |m| ≤ 8
```

上限を決める前に、通る組が何通り残るかを数えた（528 → 288通り）。
100 seed で期待される distinct は約85なので dup ≈ 0.15 と見積もり、実測 0.16 / 0.15。

> **上限は当てずっぽうで置かない。**「その上限で組が何通り残るか」を先に数えれば、
> `check_cell` を回す前に dup がいくつになるか分かる。

## ✅ S-9【致命】三角形にならない3辺（`g3_l52.find_value`/`knowledge.Lv2`）

「3辺の長さが **5cm, 12cm, 19cm**」（5+12 = 17 < 19）。「直角三角形ではない」という
**答えは正しい**が、その三角形は存在しない。S-1 とまったく同じ根で、
ピタゴラス数の斜辺に ±1〜8 を足したあと**三角不等式を見ていなかった**。

```python
def _is_triangle(a: int, b: int, c: int) -> bool:
    x, y, z = sorted((a, b, c))
    return x > 0 and x + y > z
```

200 seed 走査して 0件（前は 120 seed で3件）。実測 dup 0.13 / 0.10。

> `pythagorean_find_value.py` の二等辺三角形（`_isosceles_candidates`）には
> **同じ検査がすでにあった**（`base < 2 * equal_side`）。設計は正しく、この2つだけが
> それを持っていなかった。

## ✅ S-10 半角スペース2件（`g1_l1.knowledge.Lv2`・`g1_l15.knowledge.Lv1`）

- `LF_OPPOSITE_QUANTITY_V1` の `{{ given.statement }} を、` — statement は
  「値下がり 457円」のように**鉤括弧で閉じる語句**なので、空白を入れてはいけない。
  `LF_VERIFY_SOLUTION_V1`（`{{ given.statement }} は、`）は statement が `x = -7` という
  **式**なので空白のままでよい。**閉じ方で決まる**（D-3 / R-4 と同じ判断）。
- 「道のり 42 km を速さ 44 km/時 で進む」→「道のり42kmを速さ44km/時で進む」。
  コーパスの他の場所は「半径5cm」と書いているので、そちらに合わせた。

**走査の偽陽性も1つ減らした。** この検査は選択肢の記号（「ア 直線 イ 放物線」）を
拾って 8セル中6セルが偽陽性だった。記号と前後の空白を落としてから見るようにして、
**残るのは実物だけ**になった（＝この検査もゲートに上げられる形になった）。

---

## まだ直していないもの

**評価で記録した欠陥は、すべて直した**（D-1〜D-31 + R-1〜R-11 + S-1〜S-10）。

残っているのは「欠陥として確定していないが、見ておくべきもの」だけ:

| | 中身 | なぜ残したか |
|---|---|---|
| D-12 の裾 | 素因数分解 3822・複合立体 16320cm³・不等式の 2896円 ほか | 大半は教材として通る範囲。**個別に判断が要る**ので、一律に狭めない |
| 度数分布表の**形** | `g1_l56.calculation.Lv2` で「5時間以上6時間未満が40人中18人」のような山 | 値の**帯**は場面に合わせた（S-4）。分布の**形**は重みを乱数で振っているので不自然になりうるが、計算を学ぶ課題としては通る |
| 飾りの大きな数 | `y = -382x²`・「222回より多く投げて」・`(x + 54)(x + 75)` | knowledge セルが dup を稼ぐために差し込んでいる数。答えに効かない |
| 効かない数 | `g2_l49.find_value.Lv1`「ひし形 NF=174cm のとき ∠NOQ は」 | 与えた長さが答え（90°）に効かない。ただし**性質だけで答えさせる**のは教科書にある形 |
| 図形の飾りの長さ | `g2_l36` 122cm・`g2_l43` 339cm・`g2_l46` 23cm | 合同・平行四辺形の**性質**を問うセルで、長さは対応関係を書かせるための飾り |
| ゲート化 | `scan_big_numbers` | 走査としては動くが**合否の基準を持たない**。ゲートにするなら「答えの大きさ」側で測る設計が要る（`scan_point_names` と `scan_empty_hints` は `text_quality` に昇格済み） |

---

# 9周目の修正（2026-08-13b・`answer_size` ゲート化）

## 原則⓪を「ゲート」にした（D-32）

これまで原則⓪（答えの大きさで測る）は**セルを直すときの手**でしかなく、
歯止めが無かった。`scan_big_numbers.py` は数を並べるだけで合否を持たない。

- `engine/core/verify/answer_size.py` — **測り方の単一の真実**。
  recipe（組み直しの判定）と eval（歯止め）が同じ関数を通る。別々に持つと
  「生成側が通す形」と「検査側が落とす形」がずれる
- `engine/eval/answer_size.py` — 6つ目のゲート（`python -m engine.eval` に組み込み済み）
- `SpecLevel.answer_size_max` / `answer_size_reason` — `dup_rate_max` と同じ形の宣言
  （理由が無いとロードで落ちる）

既定の上限は **分母12 / 分子100 / 根号の中60**。整数そのものは測らない。

### recipe 側の型（5つの recipe に同じ形で入れた）

```python
limits = limits_for(ctx.spec_level)          # 宣言があればそれ、無ければ既定
for attempt in range(40):
    attempt_rng = rng.spawn(attempt) if attempt else rng
    ... 引く → solver で解く ...
    if not answer_is_too_big(display, limits):
        break                                 # 収まったら採用
# 40回で収まらなければ最後の組で通す（1セル消えるより軽い。ゲートが見つける）
```

入れた recipe: `pythagorean_find_value` / `word_problem_pythagorean` /
`simplify_radical` / `solve_quadratic` / `compute_signed_arithmetic` /
`evaluate_inverse_proportion`。**1つ入れるたびに複数セルが直った**
（三平方は 12 セル、平方根は 3 セル、二次方程式は 4 セル）。

**実測**: 三平方10セルは rejects 0/120・dup ≤ 0.13（引き直しは dup を悪くしない）。
平方根は dup ≤ 0.17、二次方程式は ≤ 0.16、正負の数は ≤ 0.14。

### 例外は累乗だけだった

累乗は底の分母をそのまま2乗・3乗するので、既定の分母12では**分数の底が1つも
使えない**（`(3/4)² = 9/16` が落ちる）。dup が 0.37/0.42 に跳ねた。

1. 上限を 216（=6³）に宣言（`(11/12)³ = -1331/1728` はこれでも落ちる）→ 0.16/0.25
2. Lv2 だけまだ 0.25 だったので、**道具を1つ増やした**（小数の底。Lv1 は前から
   持っていた）→ **0.17**。定義域は広げていない

## 量の表示を1か所に集めた（`fmt_measure`）

**同じ量が、セルによって小数と仮分数で書かれていた。**

| どこ | 前 | 後 |
|---|---|---|
| 四分位数・中央値（3 solver） | `27/2` | `13.5`（`_fmt_half` を通す） |
| 累積相対度数の割合 | `185/2%` | `92.5%`（`_fmt_relative` を通す） |
| 三角柱の体積 | `225/2 cm³` | `112.5 cm³` |
| 長方形の対角線の半分・中点連結・平行線と比 | `333/2` | `166.5` |
| 動点の三角形の面積 | `2065/2` | `1032.5` |

**量には小数、式の中の数には分数**が作法。`fmt_measure`（`solvers/arithmetic.py`）に
集約し、割り切れない値は分数のまま出す（量として書けない形を隠さない）。
srepr は変えていない＝**答えの値は1つも変わっていない**（表示だけ）。

## 宣言した25セル

確率14（分母＝場合の数）・有理化1（分母＝根号の中）・循環小数1（分母＝循環節）・
大小比較1（並べ直すだけ）・累乗2（底のn乗）・座標平面の2点間距離2（1+傾き²）・
比例定数1（停止距離）・面積比1・その他2。**すべて `answer_size_reason` に
「なぜ大きいのが正しいか」を書いた。**

## 解説を読んで見つけた分（D-33）

`scratchpad/dump_explanation_texts.py`（新規）で**文型ごとに束ねて**読んだ
（4,169手 → 1,131文型。文型を読み切れば日本語は読み切ったことになる）。

- **問いと答えの食い違い**（g1_l34.knowledge.Lv1）。正誤を問うているのに答えが
  場所の記述。問いを答えの形に合わせ、比例定数を ±500 → ±60 に戻して
  `dup_rate_max: 0.40` を宣言（実測 0.36）
- **「象限」は中学の教科書に無い**（高校で扱う）。選択肢と解説の2か所を
  「右上と左下の部分」に直した
- **「へこませて立体に組み立てられる」**（正多面体の条件）→「折り曲げて立体の頂点に
  できる」。折るのであって、へこませるのではない

---

# 10周目の修正（2026-08-13c）

## 原則⓪を問題文にも広げた（D-34）— **パイプラインで組み直す**

`answer_size` は recipe ごとに引き直しの loop を入れた（6か所）。問題文は
**どの recipe も `mr.given` に置く**ので、パイプライン1か所で済んだ:

```python
# engine/core/pipeline.py _construct_with_statement_size
limits = statement_limits_for(ctx.spec_level)
for attempt in range(40):
    attempt_rng = rng.spawn(1000 + attempt) if attempt else rng   # recipe 側の spawn と別枝
    mr = _construct_with_bounded_retry(ctx, attempt_rng, registry=registry)
    if not statement_is_too_big(" ".join(mr.given.values()), limits):
        return mr
return last          # 収まらなければ最後の組（1セル消えるより軽い。ゲートが見つける）
```

**上限内のセルは1回目で返るので、goldenは動かない**（実際に動いたのは31セル中の
上限超えだけ＝17 golden）。定義域は1つも狭めていないので **dup も動かない**
（実測: 14セルすべて 0.00〜0.13。`g2_l34` の 0.86 は前からの宣言つき）。

**単位ごとの上限**にしたのが効いた。裸の数に一律の上限は置けない（標本調査の
「76000人」・有効数字の「615cm」・内角の和の「5040°」は大きくて正しい）が、
**単位を見れば「その大きさの図形はありえない」が言える**。

## 証明の日本語（D-35）

| 前 | 後 | なぜ |
|---|---|---|
| `(10b + h) - (b + h) = 9b = 9 × b` | `= 9b` で止める | 同じ式を書き方だけ変えて並べていた。単項の商は並置（`9b`）。数字で始まる商（`2n`）は `×` のまま（`92n` と読めるため） |
| 「②、①、③より」 | 「①、②、③より」 | 前向き推論の並び順のまま出していた。`sorted` を入れ、**153問で乱れ0**を確認 |

## 逆翻訳ゲート（D-36）の道具

```
scratchpad/bt_dump.py    問題文だけを書き出す（答え・式・recipe は見せない）
scratchpad/bt/answers.tsv 読み手が日本語だけから解いた答え
scratchpad/bt_check.py    エンジンの答えと突き合わせ、**値**で比べる（表記差を吸収）
```

`bt_check.py` は「読み手の値がエンジンの答えに1つも欠けていないか」で見る。
エンジン側は単位や見出し（「12%の食塩水は500g」）で数が増えるので、集合の一致では
測れない。**日本語が数式と食い違っていれば、読み手の値のどれかが必ず外れる。**

**この検査は自動では回せない**（読み手が要る）。回帰の歯止めではなく、
**文型を足したときに1回通す関門**として使う。

## D-37 解説の途中の値（2026-08-14）

- 新規 `engine/packs/math/solvers/_step_text.py`（`top_level_parts`/`flatten_factors`/
  `fmt_expr`/`join_signed`）
- 直した solver/recipe: arithmetic・polynomial・letter_expr・quadratic・quadratic_function・
  linear・equation・radical・motion・probability・quartile・statistics_distribution・
  statistics_inquiry・distribution_chart・sample_survey・solid_view・solid_figure・
  plane_geometry・plane_transform・pythagorean・pythagorean_find_value・angle_tracking・
  inscribed_angle・triangle_properties・quadrilateral_properties・congruence_correspondence・
  similarity・similarity_scale_ratio・similarity_conditions・parallel_line_ratio・
  figure_reading・g1_space・number_proof 系・tree_diagram・rational_form・proposition_logic・
  exam_linear_figure・word_problem_pythagorean・word_problem_probability・
  word_problem_quadratic_function・word_problem_proportion_frequency・word_problem_sqrt_misc
- 走査ツール新規 `scratchpad/scan_step_values.py`（op 別・該当セル一覧つき）
- `scratchpad/approve_all.sh`（golden 281 family を1プロセスで再承認）
- `scan_explanations.py`: 「解説の分母が13以上」を削除（`engine.eval.answer_size` が
  宣言込みで測るので二重）・「括弧が指示の言い直し」に語尾の条件を足した

## D-38 図の読みやすさ（2026-08-14）

- 新規 `engine/packs/math/visuals/_label_place.py`
  （`outward`＝重心と反対側へ逃がす／`centroid`／`haloed_text`＝白フチ2枚重ね）
- `visuals/graph.py`: 目盛の数字に白フチ・`grid_tick_anchors()` を公開・
  2点の点名を重心の反対側へ
- `visuals/solid.py`: 見取図の頂点名を立体の重心と反対側へ＋白フチ
- `visuals/plane_transform.py`: 多角形の頂点名を重心の反対側へ＋目盛の位置を避ける
- `visuals/distribution_chart.py`: 度数折れ線のぶん軸を半階級広げる・目盛に白フチ
- `visuals/plane_figure.py`: 直線名が重なる間は自分の線に沿って手前へ下げる・白フチ
- 新規 `scratchpad/scan_figure_legibility.py`（重なり・はみ出し・貫通・小さい文字・点名の対応）
- 新規テスト `engine_tests/unit/test_visuals.py::test_frequency_polygon_stays_inside_the_axes`

## D-39 問題文と数式の食い違い（2026-08-14）

- `recipes/quadratic_function.py`・`solvers/quadratic_function.py`・
  `curriculum/math/families/g3_l37.find_value.yaml`:
  「2等分」→「等しくなる」／mode 名 `bisecting_point` → `equal_area_point`
- `recipes/solid_figure.py`: 「中心角を求め、表面積を求めよ」→「中心角を使って、表面積を求めよ」
- `recipes/probability.py`: 「樹形図で数え、」→「樹形図で数えて、」
- 新規 `scratchpad/bt_patterns.py`（逆翻訳の対象を文型で数え、未読だけを書き出す）
- `scratchpad/bt_check.py`: `answers*.tsv` を全部読む・`patterns.json` も index に混ぜる・
  `3a/10` と `0.3a`／`a/8` と `0.125a` を同じ値として読む・
  **「数で比べられない」を別に数える**（記号だけを答えさせるセル）
- 新規 `scratchpad/scan_asks_vs_answers.py`（読む先を絞る道具。欠陥判定はしない）
- 読み手の答え: `scratchpad/bt/answers3.tsv`（word_problem 106）・
  `answers4.tsv`（find_value 249）

## D-40 証明の解説の締め（2026-08-14）

- `recipes/geometry_proof.py`: `_claim_tail()` を足し、主張の形から
  「何が分かるか」を決める（「次がいえる」をやめた）
